---
id: "146"
slug: synthetic-captions-data-curation
style: serious
category: multimodal
difficulty: advanced
question: "Why do multimodal models train on synthetic captions instead of web alt-text?"
tags: [synthetic-data, recaptioning, alt-text, filtering, model-collapse, curation]
---

# Recaptioning: replacing the web's captions with better ones

Web alt-text is the original supervision for image-text models, and it is terrible. It is
frequently empty, an SEO keyword string, a filename, a photographer credit, or a caption about
something adjacent to the image. Even the good ones are short and mention one salient object out of
twenty.

**Recaptioning** replaces it: run a strong VLM over every image and generate a detailed description
to train on instead. This was the change that visibly improved text-to-image prompt adherence, and
it is now standard for VLM pretraining too.

```
   original alt-text:  "IMG_2024 beach"
   recaption:          "A golden retriever running along a wet sand beach at sunset,
                        a red frisbee in its mouth, two people walking in the distance,
                        long shadows, overcast sky above the horizon."

   the model can now learn attributes, counts, spatial relations and context
   that the original caption could not have taught it
```

## Why it works

The training signal is now **dense and aligned**. Every phrase corresponds to something actually in
the image, so the model learns attribute binding, relations and counting — precisely the
capabilities that fail when trained on alt-text (questions 120, 128). For generation, prompt
adherence improves because the training captions now resemble what users actually write:
descriptive sentences, not keyword strings.

## The mixing rule nobody should skip

Train exclusively on synthetic captions and the model learns the **captioner's** distribution — its
phrasing, its style, its blind spots — and then fails on real user prompts, which are short, messy
and ungrammatical.

The standard fix is a **blend**: keep a proportion of original captions. DALL-E 3 reported roughly
95% synthetic / 5% original; other recipes use more original. The exact ratio is corpus-specific and
worth ablating, but the *principle* is not optional: **some real captions must survive** or you have
trained a model that only understands one describer.

Length matters too. Mix short and long synthetic captions, or the model will not handle short
prompts — a model trained only on 60-word descriptions produces poor results from three words.

## The failure mode that poisons everything

**The captioner hallucinates, and the hallucination becomes ground truth.**

A VLM writing captions gets objects wrong (question 122). Those errors do not stay local — they
become training labels, and the next model learns them as fact. Worse, the errors are *systematic*:
the captioner has consistent biases, so the same wrong association is reinforced across millions of
examples rather than averaging out as noise.

Controls that actually help:

* **Verify a sample by hand.** Read 200 captions. This is unglamorous and catches more than any
  automatic filter.
* **Filter by image-text agreement** with a *different* model than the one that captioned, so you
  are not asking a model to check its own work.
* **Prefer short factual captions over long florid ones** where accuracy matters; hallucination
  scales with caption length exactly as it does with any generation length.
* **Keep the captioner's identity in the metadata** so you can trace and re-caption later.

## The broader curation pipeline

Recaptioning is one stage. Around it: deduplication (near-duplicate images inflate their captions'
influence), NSFW and CSAM filtering (non-negotiable, before anything else), face and PII handling,
licence filtering, aesthetic and resolution filtering for generation, and decontamination against
evaluation sets — which for image data means perceptual hashing, not exact match.

## On model collapse

Training on generated data can degrade a model when generations replace real data across
generations of training. Recaptioning is **not** that case: the *images* remain real, and the
captions are generated by a model conditioned on those real images. Information is being
transferred from a stronger model to a weaker one, not invented from nothing. The risk is
inheriting the captioner's biases and errors — real, and different from collapse. Be precise about
which one you are worried about.

## What an interviewer digs into next

* Why keep any original alt-text at all?
* Why do captioner hallucinations hurt more than random label noise?
* Why filter with a different model than the one that captioned?
* Is recaptioning model collapse? Defend your answer.
